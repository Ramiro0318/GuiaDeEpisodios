<UserControl x:Class="GuiaDeEpisodios.Views.TemporadaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuiaDeEpisodios.Views" xmlns:viewmodels="clr-namespace:GuiaDeEpisodios.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:GuiaEpViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10,0,10,0" Background="#02FFFFFF">
        <TextBlock Text="{Binding Nombre}" TextWrapping="Wrap" FontWeight="Black" FontSize="30"></TextBlock>
        <Image Source="{Binding Imagen}" Margin="12" Height="180" Width="120"></Image>
    </StackPanel>
</UserControl>
